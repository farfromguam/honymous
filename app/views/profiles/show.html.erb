<h1><%= @profile.name %></h1>
<h3><%= @profile.one_liner %></h3>

<p><%= @profile.description %></p>

<p>
  <%= @profile.name %> is open to:
  <% if @profile.general_comments == true %>
    General Comments
  <% end %>
  <% if @profile.criticism == true %>
    Constructive Criticism
  <% end %>
  <% if @profile.about_myself == true %>
    Hearing about Myself
  <% end %>
  <% if @profile.anything == true %>
    Anything and Everything
  <% end %>
</p>


<h3>The word on the street:</h3>
<% @profile_comments.each do |e| %>
  <p><%= e.comment %><br>

  [ Count | Vote Up | Vote Down ]

  </p>
<% end %>


<h3>Add your two cents:</h3>
<%= form_for [@profile, @profile.comments.new] do |f| %>
<%= f.label :comment, "Your Comments" %>
<%= f.text_area :comment, :cols => 35, :rows => 5 %><br>

<% if user_signed_in? %>
  [ ] Submit as Logged in user
<% else %>

  <%= #f.text_field :ip_address, :value => request.remote_ip
  %>

  This will be submitted anonymously,<br>
  <%= link_to('Log In', new_user_session_path) %> to submit as a real person

<% end %> <br>

<p>
  <%= f.submit "Submit" %>
</p>
<% end %>
